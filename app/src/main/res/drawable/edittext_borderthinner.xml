<!-- drawable/edittext_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="#BCABFA" /> <!-- Color lila (puedes cambiar el valor #BCABFA por el color lila que desees) -->
    <corners android:radius="8dp" /> <!-- Bordes redondeados de 5dp (puedes ajustar el valor según tus preferencias) -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp" /> <!-- Padding de 15dp en todas las direcciones -->
</shape>
